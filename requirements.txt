# PixMatch - runtime dependencies
#
# Phases 1-2 (current): recursive scanning, SHA-256 exact-duplicate detection,
# pixel-by-pixel comparison, perceptual hashing (pHash/dHash/aHash),
# SQLite analysis cache, PySide6 GUI with side-by-side + advanced comparison.
#
# Perceptual hashing is implemented in-project with Pillow only (no numpy /
# imagehash) - see README "decisiones tecnicas".
#
# Later phases add: send2trash (recycle bin, Phase 3) and FFmpeg-based video
# analysis (Phase 4; FFmpeg is an external binary, not a pip package).

PySide6>=6.11.2,<7
Pillow>=12.3.0      # note: getbbox(alpha_only=...) is used, needs Pillow >= 9.2
Send2Trash>=2.1.0  # Phase 3 - move files to the OS recycle bin (never a plain delete)

# --- optional, enabled automatically when present ---
# Video analysis (Phase 4) needs FFmpeg. Preferred: install the system FFmpeg
# (`apt install ffmpeg`, `brew install ffmpeg`, `winget install Gyan.FFmpeg`)
# and, if needed, set its path in Settings. As a zero-config fallback the app
# also uses the binary bundled by imageio-ffmpeg:
# imageio-ffmpeg>=0.5    # bundled FFmpeg binary (~30 MB), fallback only
#
# pillow-heif>=0.16      # HEIC / HEIF support
# pillow-avif-plugin>=1.4 # AVIF support

# --- development / testing ---
pytest>=8.0
