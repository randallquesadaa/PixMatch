"""PixMatch - a desktop tool for finding duplicate and near-duplicate images
and videos, plus tools to rename, import and organize a photo/video library.

The application never modifies, moves, renames or deletes files on its own.
Every destructive action (delete, rename, import, organize) requires an
explicit, confirmed decision from the user and is written to an undoable
operation history.
"""

__version__ = "0.8.0"  # + import/organize-into-library and organize-in-place tools
APP_NAME = "PixMatch"
ORG_NAME = "PixMatch"
